# Development-server proxy target. This value is not exposed to browser code.
VITE_API_TARGET=http://127.0.0.1:8000

# Optional public API prefix used by browser requests. Keep empty when using the Vite proxy.
VITE_API_BASE_URL=

